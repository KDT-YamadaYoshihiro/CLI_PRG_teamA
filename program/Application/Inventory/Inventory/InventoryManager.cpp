#include "InventoryManager.hpp"

std::vector<std::string> Inventory::InventoryManager::GetAllNames()
{
    // 保存しているコレクションから名前だけを抜き出して
    // 新しい名前だけのコレクションを作成する

    return std::vector<std::string>();
}
